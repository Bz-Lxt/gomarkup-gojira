-- Demo accounts, project, sprint and issues are seeded in Go
-- (internal/seed) so bcrypt hashes stay consistent and the seed is idempotent.
-- This migration is reserved for SQL-only helpers / trigger templates.
SELECT 1;
