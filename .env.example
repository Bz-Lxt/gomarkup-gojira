# Copy to .env for host-side overrides. Compose already injects defaults.
TZ=Asia/Shanghai
JWT_SECRET=gojira-dev-secret-change-in-prod-32b

# Switch Mailpit → real SMTP (see README §7 after /deploy):
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=secret
# SMTP_FROM=noreply@example.com
# SMTP_TLS=true
